{{> header}}
<div class="container overflow-hidden">
    <div class="row gx-5 justify-content-center pt-5">
        <div class="col-8">
            <div class="m-2">
                <p class="fw-bold fs-1">Search results for: "{{search}}"</p>
                {{#if filter}}
                    {{> establishment-list}}
                {{else}}
                    {{> review-list}}
                {{/if}}

                {{#if hasnoEst}}
                    <div class="text-muted">No results to display</div>
                {{/if}}
                <div class="container-fluid p-0" >
                    {{#if hasPrev}}
                    <a type="button" class="btn btn-success mb-5" href="/search/{{prevNum}}">
                        <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" fill="currentColor" class="bi bi-caret-left-fill" viewBox="0 0 16 16">
                        <path d="m3.86 8.753 5.482 4.796c.646.566 1.658.106 1.658-.753V3.204a1 1 0 0 0-1.659-.753l-5.48 4.796a1 1 0 0 0 0 1.506z"/>
                        </svg>
                        Previous
                    </a>
                    {{/if}}
                    {{#if hasNext}}
                    <a type="button" class="btn btn-success float-end mb-5" href="/search/{{search}}/{{nextNum}}">
                        Next
                        <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" fill="currentColor" class="bi bi-caret-right-fill" viewBox="0 0 16 16">
                        <path d="m12.14 8.753-5.482 4.796c-.646.566-1.658.106-1.658-.753V3.204a1 1 0 0 1 1.659-.753l5.48 4.796a1 1 0 0 1 0 1.506z"/>
                        </svg>
                    </a>
                    {{/if}}
                </div>
            </div>
        </div>
    </div>
</div>